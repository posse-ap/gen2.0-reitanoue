name: PHPUnitの実行
on:  push
  branches:
    - ph4_test
jobs:
  phpunit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: ハローワールド
        run: echo "Hello world"
